{
  // MCPサーバーの例設定。必要に応じて公式サーバーや自作サーバーの設定に書き換えてください。
  "servers": [
    {
      "name": "filesystem-mcp-server",
      "type": "stdio",
      "command": "npx",
      "args": [
        "@modelcontextprotocol/server-filesystem",
        "/workspaces/codex-ultimate-prompt"
      ]
    },
    {
      "name": "mcp-installer",
      "type": "stdio",
      "command": "npx",
      "args": ["@anaisbetts/mcp-installer"]
    }
  ]
}
